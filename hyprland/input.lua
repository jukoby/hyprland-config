hl.config({
    input = {
        kb_layout = "de",
        numlock_by_default = true,

        sensitivity = 0,
        follow_mouse = 1,
        -- accel_profile = "flat"

        kb_options = "compose:sclk,fkeys:basic_13-24"
    },

    cursor = {
        default_monitor = "DP-1"
    }
})